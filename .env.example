# ============================================================================
# Yigcore Sentinel - Environment Configuration
# ============================================================================
#
# Copy this file to .env and customize for your environment
#   cp .env.example .env
#
# ============================================================================

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------

# Port for Sentinel HTTP API (default: 11435)
SENTINEL_PORT=11435

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# ----------------------------------------------------------------------------
# Governance Settings
# ----------------------------------------------------------------------------

# Default budget per user in dollars (default: 10.0)
DEFAULT_BUDGET=10.0

# Rate limit: max requests per second (default: 100)
RATE_LIMIT_CAPACITY=100

# Rate limit refill rate: tokens added per second (default: 10)
RATE_LIMIT_REFILL_RATE=10

# Path to custom policy file (default: ./policy.json)
# POLICY_PATH=/path/to/custom-policy.json

# ----------------------------------------------------------------------------
# OpenClaw / AI Agent Configuration (if using docker-compose)
# ----------------------------------------------------------------------------

# API keys for LLM providers
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# ----------------------------------------------------------------------------
# Advanced Settings
# ----------------------------------------------------------------------------

# Node.js environment (development, production)
NODE_ENV=production

# Host binding (0.0.0.0 for Docker, localhost for local dev)
HOST=0.0.0.0
